#include <iostream>

using std::cin;
using std::cout;

int main() {
    int number;
    cin >> number;
    double result = 0;
    double sign = 1;
    for (int i = 0; i < number; i++) {
        result += sign / (i + 1);
        sign = -sign;
    }
    cout << result;
}
